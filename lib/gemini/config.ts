/**
 * Model names are isolated behind named constants so upgrading tailoring
 * quality later (e.g. to a "pro" model) is a one-line change.
 */
export const TAILOR_MODEL = "gemini-2.5-flash";
export const TEST_CONNECTION_MODEL = "gemini-2.5-flash";
